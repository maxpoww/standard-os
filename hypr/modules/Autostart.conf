# Startup programs

exec-once = ~/bar.sh &

exec-once = hyprpaper

# Waybar is started as a systemd --user unit (services.waybarBar.systemd.enable).
# Keeping the launch.sh exec-once here would race the unit and double-instance.

exec-once = gnome-keyring-daemon --start --foreground --components=pkcs11,secrets,ssh

